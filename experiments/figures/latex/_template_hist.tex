% Generic histogram / CDF template.
% For binv_per_key, gamma_root_rejection, lemma9, etc.
%
% The CSV is expected to have at least two columns: a value column (e.g. `x`
% or `mag`) and a CDF/density column (e.g. `cdf` or `count`). For now,
% the experiments don't write CSVs in this shape — to use this template,
% migrate the corresponding experiment to also write its histogram data.

\begin{tikzpicture}
\begin{axis}[
    width=0.85\linewidth, height=0.50\linewidth,
    xlabel={\textsf{Quantity}},
    ylabel={\textsf{empirical CDF}},
    grid=both,
    grid style={line width=.1pt, draw=gray!30},
    major grid style={line width=.2pt, draw=gray!50},
    ymin=0, ymax=1,
    legend cell align={left},
    legend pos=south east,
]

\addplot+[mark=none, very thick] table
    [x=val, y=cdf, col sep=comma]
    {../../tables/CSV_NAME.csv};
\addlegendentry{Empirical CDF}

% Optional vertical thresholds.
% \draw[dashed, red, thick]    (axis cs:32, 0) -- (axis cs:32, 1)
%     node[pos=0.5, right] {current};
% \draw[dashed, green, thick]  (axis cs:25, 0) -- (axis cs:25, 1)
%     node[pos=0.5, right] {proposed};

\end{axis}
\end{tikzpicture}
